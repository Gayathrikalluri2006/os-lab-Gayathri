#include<stdio.h>

int main()
{
    int block[5]={100,500,200,300,600};
    int process=212;

    for(int i=0;i<5;i++)
    {
        if(block[i]>=process)
        {
            printf("Allocated in Block %d\n",i+1);
            break;
        }
    }

    return 0;
}
