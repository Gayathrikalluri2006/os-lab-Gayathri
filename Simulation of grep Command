#include<stdio.h>
#include<string.h>

int main()
{
    char str[100],word[20];

    printf("Enter String: ");
    gets(str);

    printf("Enter Word: ");
    scanf("%s",word);

    if(strstr(str,word))
        printf("Found\n");
    else
        printf("Not Found\n");

    return 0;
}
